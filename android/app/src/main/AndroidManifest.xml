<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.foresty"> <!-- Substitua "com.example.foresty" pelo nome do seu pacote (package) definido no pubspec.yaml -->

    <application
        android:label="foresty"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">

        <!-- ... outras configurações ... -->

        <!-- Configuração para o Facebook Login -->
        <activity
            android:name="com.facebook.FacebookActivity"
            android:configChanges="keyboard|keyboardHidden|screenLayout|screenSize|orientation"
            android:label="@string/app_name" />

        <activity
            android:name="com.facebook.CustomTabActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <!-- Substitua "FACEBOOK_APP_ID" pelo seu App ID fornecido pelo Facebook for Developers -->
                <data android:scheme="fbFACEBOOK_APP_ID" />
            </intent-filter>
        </activity>

        <!-- Configuração para o Firebase Authentication -->
        <!-- Substitua "FACEBOOK_APP_ID" pelo mesmo App ID fornecido pelo Facebook for Developers -->
        <meta-data
            android:name="com.facebook.sdk.ApplicationId"
            android:value="@string/facebook_app_id" />

        <!-- ... outras configurações ... -->

    </application>
</manifest>
